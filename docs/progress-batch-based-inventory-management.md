# Progress: batch-based-inventory-management

Source: docs/requirements/2026-07-08-batch-based-inventory-management.md
Started: 2026-07-08

- [x] 1. Classifier — task_size=full, branch=feature/fefo-batch-inventory
- [x] 2. CEO — brief written, flow_type=backend-feature, approach: custom module + plugin/observer on deduction path (not MSI SourceItem reuse)
- [x] 3. Architecture — plugin on SourceDeductionServiceInterface::execute(), SELECT...FOR UPDATE locking, 2 new tables (rgd_inventory_batch, rgd_inventory_batch_transaction). Approved.
- [x] 4. Eng — spec at docs/spec-batch-based-inventory-management.md, caught/fixed transaction-ownership inconsistency in self-review
- [x] 5. Design — skipped (backend-feature, no customer-facing UI)
- [x] 6. Build — Rgd_Inventory module implemented (28 files: 8 API/Data interfaces, 10 Model implementations, 1 Plugin, db_schema.xml with 2 tables, di.xml/module.xml/acl.xml, i18n, README/BUILD_SUMMARY docs). setup:upgrade + di:compile passed.
- [x] 6b. Post-build correction — expiry cutoff flipped to exclude batches expiring today (expiry_date > CURDATE(), not >=). CSV import deferred as a follow-up (not phase 1).
- [x] 7. Unit Testing — 17/17 module tests pass; full suite 22/22. Fixed pre-existing composer.json exclude-from-classmap bug that broke phpunit entirely.
- [x] 8. QA/Scenario Testing — found 6 blocking bugs (module never registered, db_schema.xml wrong attribute so tables never created, BatchSearchResultsInterface extended concrete class, broken virtualType factories, FEFO ORDER BY SQL syntax broken = core feature non-functional). Applied fixes to unblock; all 11 scenario tests pass after fixes.
- [x] 9. Security Testing — 0 CRITICAL, 1 HIGH (batch deduction committed before MSI's proceed() — cross-transaction integrity risk), 2 MEDIUM (no negative-qty floor guard, no validation in BatchRepository::save()), 4 LOW (forward-looking ACL/CSRF notes for not-yet-built admin UI). No SQL injection found.
- [x] 10. Performance Testing — 1 CRITICAL (3-4 redundant queries per item on hot deduction path), 5 Warning (missing source_code in index causing filesort, lock held across proceed(), N+1 order-item lookup, duplicated/unscoped FefoBatchSelector queries, unbounded report table missing indexes), 2 Info (dead-weight index column, unbounded per-SKU fetch).
- [x] 11. Bug Fixing (3 passes total):
  - Pass 1 (triggered by QA): all 6 wiring/schema/SQL bugs formally verified fixed, +2 more found (stale composer autoload dump, `-o` optimized-mode incompatibility). Regression 22/22.
  - Pass 2 (triggered by Security): transaction atomicity fixed (coordinator owns one shared tx across all items + MSI's proceed()), floor guard added, save() validation added. Also caught a separate pre-existing critical bug: `$item->getQtyToDeduct()` called a nonexistent method — plugin would have fatal-errored on every real checkout. 37/37 tests (15 new), real-DB rollback verified.
  - Pass 3 (triggered by Performance): 7/8 fixed (redundant queries removed, index restructured to cover source_code, report table indexed, N+1 order-item lookup batched into 1 query, lock-order documented). #8 (LIMIT on FEFO fetch) correctly skipped — would risk false insufficient-stock errors for minor gain. EXPLAIN confirmed index fix works. 37/37 tests pass.
- [x] 12. Code Review — verdict: NOT ready. 2 CRITICAL (duplicate-key detection checks SQLite error text, broken on production MySQL; admin CRUD UI entirely missing from spec'd phase 1), 6 Warning (dead CollectionFactory dependency failing phpstan, stale whitelist, order_id never persisted to ledger, README describes pre-fix behavior, leaky AdapterInterface on @api interface, duplicated diagnostic logic), 4 Info. All 3 prior fix rounds verified to hold.
- [x] 12b. Build — admin UI + Code Review findings. SELF-VERIFICATION FAILED (claimed ready without running tests). Independently found 6/37 tests failing (FefoDiagnostics extraction broken).
- [x] 11d. Bug Fixing (4th pass) — reverted FefoDiagnostics extraction (root cause: injected as mock in tests but never wired, silently no-op'd instead of throwing). Verified with real pasted output: 37/37 pass. I independently re-ran phpcs (clean) and phpstan (44 errors — investigated: ~35 are known Magento+phpstan false positives for auto-generated Factory classes + missing generic array type-hints, but 2 real bugs found: ->setPath() called on generic ResultInterface (6x), non-string passed to addErrorMessage/addSuccessMessage (7x) — real strict_types=1 risk. Dispatching pass 5 for these + proper phpstan.neon.dist ignoreErrors for the known false positives.
- [x] 11e. Bug Fixing (5th pass) — fixed 13 real phpstan bugs (setPath() on wrong type x6, Phrase-vs-string x7), added properly-scoped phpstan.neon.dist ignores for 2 legitimate Magento false-positive categories, explicitly left 3 unrelated out-of-scope findings unfixed/unignored rather than overstepping. Independently re-verified: 37/37 tests, phpstan down to 3 documented remaining items.
- [x] 13a. Approver (1st pass, Sonnet) — BLOCKED. Caught: unresolved dead AdapterInterface import (Code Review Warning 5 claimed fixed, wasn't fully), phpmd never run (1 of 4 make-check sub-gates skipped), requirement Status field never updated, no persisted real-DB scenario-verification artifact. Recommended escalating final approval to Fable given patient-safety-adjacent stakes. I fixed the dead import and Status field directly; dispatching final pass for phpmd + remaining phpstan + scenario artifact.
- [x] 11f. Bug Fixing (6th pass) — phpmd 19→0 findings (fixed MissingImport, ElseExpression, extracted 10-param writeTransaction() into a DTO, reduced complexity/coupling with documented suppressions where appropriate), phpstan 6→0 errors (2 genuine dead-code fixes, 3 in-code docblock fixes, 2 narrowly-scoped justified ignores), produced docs/qa-scenario-verification-batch-based-inventory-management.md (real MySQL run, 11/11 checks pass). ALSO CAUGHT ANOTHER REAL BUG during real-DB verification: FefoBatchSelector's diagnostic message used fetchOne() where COUNT(*) was needed, producing nonsense values ("1227 batch(es)") in admin-facing error text — invisible to mocked unit tests. Fixed. Independently re-verified: 37/37 tests, phpstan clean, phpmd clean.
- [x] 13b. Approver (2nd pass, Fable) — APPROVED for Phase 1 shipping. All 4 gates independently re-verified clean (37/37 tests, phpcs/phpstan/phpmd all clean). All 4 first-pass gaps confirmed closed. Both Code Review CRITICALs and all 6 Warnings spot-checked resolved. No unresolved BLOCKER/CRITICAL/HIGH anywhere in history. Scope note: Phase 1 only (returns stubbed, CSV import deferred, both documented). Does not authorize commit/merge — Review and Ship stages still required.
- [x] 14a. Committed — c50dfb0 on feature/fefo-batch-inventory, 54 files, 5718 insertions. Pre-commit hook ran phpcs+phpstan on real staged files, passed.
- [x] 14. ADR — docs/adr/1-fefo-batch-inventory-architecture.md. One ADR covering all 5 interrelated decisions (MSI integration approach, locking strategy, transaction boundary — including the real HIGH-severity bug Security found and how it was fixed, returns strategy, ledger authority). Status: Accepted.
- [x] 15. Documentation — reviewed/fixed module README (corrected stale claim about per-item transactions — the exact HIGH bug that was fixed; corrected admin menu path; added admin UI usage + Known Limitations sections), added Rgd_Inventory as a real-world example to docs/04, added ADR section + per-change-docs convention to docs/README. Found one more minor accuracy gap: identity fields render as inputs but are silently discarded server-side on edit (not disabled) — noted in README, not a functional bug.
- [x] 16. PR — docs/pr-batch-based-inventory-management.md. Honest summary including the 5 headline bugs caught + fixed, test results, backward-compat, Phase 1 scope note, migration notes. (First attempt hit a transient API connection error and produced nothing; retried cleanly.)

## Flow complete — all 16 stages done (12 ran, 3 skipped/conditional as designed: Design not applicable, Bug Fixing ran 6x since it's conditional-and-repeatable, ADR ran since warranted)

---

# Run 2 — fresh full run, expanded scope (2026-07-10)

Source: docs/requirements/2026-07-08-batch-based-inventory-management.md (Status field manually reset to "Ready for Implementation" in working tree)
Started: 2026-07-10

Scope note: branch `feature/fefo-batch-inventory` carries uncommitted work beyond
Run 1's shipped commit (`c50dfb0`) — GraphQL schema/resolvers, admin batch listing
data provider, admin UI fixes (requirejs collapse mixin), plus doc/script edits.
User directed a fresh full 16-stage run treating the requirement + all current
uncommitted work as the task scope, rather than only validating the delta.

- [x] 1. Classifier — task_size=full, branch=feature/fefo-batch-inventory (continue existing)
- [x] 2. CEO — brief written, flow_type=backend-feature. Flagged: GraphQL surface contradicts shipped PR's "no new REST/GraphQL surface" Phase-1 boundary; unauthenticated batch expiry/qty exposure needs explicit decision; no test file for the resolver; Architecture stage recommended required (not skipped)
- [x] 3. Architecture — decided: `@cache(cacheable: false)` on rgdInventoryStock (not IdentityInterface+tags, to avoid coupling FPC invalidation into locked SourceDeductionCoordinator write path); GraphQL boundary reopening approved as read-only/additive amendment to ADR-1; getAvailableBatches() co-located on FefoBatchSelectorInterface accepted; ADR addendum content drafted for stage 14. Approved by user.
- [x] 4. Eng — spec at docs/spec-batch-based-inventory-management-run2.md; made the @cache(cacheable: false) edit to schema.graphqls directly; identified real coverage gap (zero tests for InventoryStock resolver; getAvailableBatches() expiry-cutoff not directly asserted through its own entry point); flagged pre-existing Run 1 spec/shipped index-name drift as a doc bug, not a code bug
- [x] 5. Design — skipped (flow_type=backend-feature per CEO brief; admin UI changes are corrective, restoring already-spec'd Phase-1 CRUD behavior and fixing a JS load-order bug, not new UX; GraphQL is a backend contract)
- [x] 6. Build — verified existing GraphQL/admin-UI code matches spec; closed both Eng-flagged test gaps (new Test/Unit/Model/Resolver/InventoryStockTest.php, 8 methods; added expiry-today-cutoff test through getAvailableBatches()'s own entry point); fixed float-cast bug on available_qty (array_sum([]) returns int, schema wants Float!). 49/49 tests, phpcs/phpstan/phpmd clean (self-reported by Build, will be independently re-verified at stage 7+)
- [x] 7. Unit Testing — independently re-verified Build's claims (ran real phpunit, confirmed tests are meaningful not tautological, confirmed float-cast bug was real by empirically reproducing the failure). Corrected Build's inaccurate self-report (claimed 1 new getAvailableBatches() test, actually 4). Found and closed a real gap: BatchListingDataProvider.php had zero coverage, added 5 tests. Final: 54/54 tests, 166 assertions, phpcs/phpstan/phpmd all clean
- [x] 8. QA/Scenario Testing — real live verification (MySQL + curl + authenticated admin HTTP + object-manager bootstrap), not mocks. All 5 scoped areas PASS: GraphQL query matches FEFO/expiry-cutoff live (incl. cache-bypass proven by direct DB mutation + re-query), admin grid load/paginate/sort/filter against real rows, admin form load+save+validation with real DB writes + ledger rows, read/write FEFO consistency directly proven via real deduct() call, original acceptance criteria re-confirmed with no regression (checkout-path files untouched by this run). No BLOCKER/CRITICAL. One low-severity dead-code note (unused requirejs map entry + orphaned JS file, zero functional effect) — non-blocking cleanup item.
- [x] 8a. Live bug (user-reported, out-of-band `/fix`) — immediately after Stage 8 closed, user reported "Add New Batch" showing an infinite spinner in their own browser. This is exactly the gap Stage 8's browser-verification note had flagged. Reproduced live (Claude-in-Chrome): grid loaded fine, but the New Batch form hung on the spinner with no console error. Root cause: stale `Magento\Ui\Config\Data` (`config`-type) cache still serving the *pre-refactor* `rgd_inventory_batch_form.xml` (old `templates/form/default` + imperative buttons array) from before the same-day declarative-form refactor — Magento's `config` cache does not auto-invalidate on ui_component/layout XML edits. The stale template bound its Knockout `scope` to an unregistered node (`rgd_inventory_batch_form.areas`), so `uiRegistry.get()` waited forever with no error — matching the reported symptom exactly. Fix: `bin/magento cache:flush` (no `app/code` change needed — the XML/PHP were already correct). Verified two ways: (1) bug-fixer agent's direct PHP reproduction of the rendering pipeline before/after the flush (template/buttons/scope all changed old→new, 49/49 tests still pass), (2) my own live re-verification in the browser post-fix — navigated Catalog → Batch Inventory → Add New Batch, form now renders all fields (SKU, Batch Number, Source, Expiry Date, Received/Remaining Qty, Active) with no console errors. Logged in `BUGS.md` under Fixed, including an operational note to always `cache:flush` after ui_component/layout XML edits. Does not require a formal Bug Fixing (stage 11) slot — handled via the project's separate `/fix` mechanism; stage 11 remains conditional on stages 9-10 findings.
- [x] 9. Security Testing — scope: new-since-Run1 surface only (GraphQL query+resolver, admin listing/form, getAvailableBatches()). No CRITICAL/HIGH. 3 MEDIUM: (M1) admin grid's async `mui/index/render` data endpoint has no `<aclResource>` in rgd_inventory_batch_listing.xml, falls back to `Magento_Backend::admin` — a role without `batch_view` could still pull grid data directly; (M2) getAvailableBatches() has no LIMIT/pagination and the GraphQL field is uncached+unauthenticated by design — unbounded-row DoS/amplification surface; (M3) Batch/Save.php applies zero server-side validation (relies entirely on client-side UI rules) — direct POST can persist negative qty, remaining_qty > received_qty, malformed expiry_date, bogus source_code. 2 LOW: (L1) batch_number/received_at exposed to anonymous GraphQL callers (minor business-intel leak, confirm intended); (L2) Delete action's grid link renders as a GET href while the controller is POST-only (HttpPostActionInterface) — flagged as a likely functional break for Stage 10/12 to confirm, not a security issue itself. INFO: SQL injection/CSRF/mass-assignment/deserialization/error-leakage/JS-safety all clean (bound params throughout, form-key enforced, whitelisted setters only, no unserialize(), no stack-trace leakage). Verdict: CLEAR TO PROCEED, no blockers.
- [x] 10. Performance Testing — scope: new-since-Run1 surface only. No CRITICAL. 2 Warning: (W1) BatchDataProvider.php's "Add New Batch" path loads the entire unfiltered batch collection with full hydration on every open (early-return-on-empty-filter case never returns `[]`) — harmless today, scales badly as the append-only batch table grows; (W2) FefoBatchSelector::getAvailableBatches() has no addFieldToSelect() column-scoping (unlike its sibling selectForDeduction()) and no LIMIT/pagination — feeds directly into Security's M2 (unauthenticated, uncached GraphQL DoS surface), and its `expiry_date IS NULL` sort-key ordering forces a filesort over the full candidate set rather than breaking early. 3 Info (clean): index coverage for the seek is unchanged/no regression (db_schema.xml untouched this run), admin grid has no N+1 (paginated, no product join) though several filter columns aren't index-backed (admin-only/low-frequency, acceptable), no duplicate/redundant queries within a request on either the resolver or save path. Verdict: CLEAR TO PROCEED, no blockers. W1 and W2 handed to Stage 11.
- [x] 11. Bug Fixing — fixed all 5 findings from stages 9-10. M1: added `aclResource: Rgd_Inventory::batch` to rgd_inventory_batch_listing.xml's dataProvider config (had to correct own first attempt after empirically proving the module's raw `configurableObject` argument style resolves ACL from a different XML position than core's typed `dataProvider class=`/`settings` style; verified via real object-manager bootstrap against Magento\Ui\Config\Data). M2+W2: getAvailableBatches() now scoped via addFieldToSelect() (matches resolver's actual field reads) + capped at 500 rows via new MAX_AVAILABLE_BATCHES const; selectForDeduction() deliberately left uncapped (unchanged). M3: added server-side validateBatch()/isValidExpiryDate() to Batch/Save.php (found BatchRepository::save() already covered qty/expiry-format validation from Run 1 — this was a Stage 9 scoping blind spot on an unchanged file; genuine new gap was batch_number empty/length + expiry_date empty, now closed) — throws InputException, caught by existing error-handling chain. W1: BatchDataProvider now short-circuits to `[]` on "Add New Batch" via a hasRealFilter flag instead of loading the full table; edit path unaffected (regression-tested). L2: confirmed a REAL bug during investigation (not just a false alarm) — grid's Delete action was missing `'post' => true`, so Magento's actions.js would GET instead of POST against a POST-only controller, silently failing; fixed in Actions.php. L1 left untouched per instructions (business-decision item, not a defect, for Approver/Docs to note). Verified: 56/56 module tests (171 assertions, +7 new), 61/61 full-repo suite, phpcs/phpstan/phpmd all clean. Noted: live authenticated curl hit an unrelated environment/session quirk affecting all admin pages equally (confirmed via core's own admin/catalog/product/index showing the same behavior) — not a regression from this fix; recommend one live click-through during Code Review/QA to close the loop on M1's ACL wiring specifically.
- [x] 12. Code Review — independently re-ran all 4 quality gates (not trusted from self-report): 56/56 tests/171 assertions, phpcs/phpstan/phpmd all clean. Independently re-verified every Stage 9-11 finding is genuinely fixed by reading the diffs — for M1 (ACL), traced Magento core's own Render.php/AbstractDataProvider.php ACL check path directly rather than trusting the fixer's self-report, confirmed the aclResource placement genuinely works. Confirmed GraphQL surface matches the approved Architecture decision (unauthenticated, cacheable:false, no leaked model objects). Confirmed admin CRUD complete per requirement (list+CRUD+validation); confirmed NOT implemented: Product Name column/filter, CSV/XLSX export, Reports, frontend batch traceability (documented Phase 2 deferral, not a regression). 2 Warning: (W1) requirement's Status/acceptance-criteria don't reflect the Phase 1/2 split — needs explicit Approver sign-off that current scope is intentional; (W2) source_code still has no server-side validation (residual of M3) — direct POST could persist a bogus source_code. 4 Info: (I1) dead orphaned rgd_inventory_batch_form.js + redundant requirejs map entry, still present; (I2) 7 new __() strings not added to i18n/en_US.csv; (I3) BUGS.md "Open" section still has a stale placeholder duplicating a resolved Fixed entry; (I4) minor server/client validation asymmetry on received_qty===0 (cosmetic, harmless). Verdict: READY for Approver, no blockers. W2/I1/I2/I3 handed to one more quick Bug Fixing pass before Approver; W1 is an Approver-level scope decision, not a code fix.
- [x] 11g. Bug Fixing (cleanup pass, pre-Approver) — closed all 4 items from Stage 12. W2: source_code now validated server-side via real `SourceRepositoryInterface::get()` lookup (not just checked against the form's single hardcoded option, since FefoBatchSelector/SourceDeductionCoordinator already couple to real dynamic MSI source codes elsewhere) — new SaveTest.php adds the controller's first-ever unit coverage (3 tests). I1: deleted orphaned rgd_inventory_batch_form.js and the dead requirejs map entry, collapse-plugin mixin fix left untouched. I2: added 9 missing i18n entries (7 flagged + 2 more from the W2 fix). I3: removed BUGS.md's stale Open placeholder. Verified: 59/59 module tests (182 assertions), 64/64 full-repo suite, phpcs/phpstan/phpmd all clean.
- [x] 12a. Live bug (user-reported, out-of-band `/fix`) — user reported "Expiry Date shows required even after selecting a date" on Add New Batch, right after Stage 12 closed. Browser access was blocked by an external policy throughout this investigation, so root-caused entirely via direct code reading. First pass: rgd_inventory_batch_form.xml's expiry_date field never set an explicit dateFormat option, so Magento's date.js component defaulted outputDateFormat to MM/dd/y while both Batch/Save.php::isValidExpiryDate() and the pre-existing (Run 1) BatchRepository::validateExpiryDateFormat() require strict Y-m-d — added `<option name="dateFormat">y-MM-dd</option>` to the field's formElements/date/settings/options. User retested and still hit the same error. Second pass: independently confirmed via a live Magento\Ui\Config\Data render that the compiled config correctly carried the new dateFormat option (ruling out a cache repeat), then proved by direct testing that isValidExpiryDate()'s byte-exact string comparison was still too brittle for real date-picker output (a trailing time component or non-padded value both still fail strict equality despite being unambiguously valid dates). Hardened the boundary instead of continuing to chase the exact client format: added normalizeExpiryDate() to Save.php, parsing the posted value leniently via PHP's DateTime constructor and re-emitting canonical Y-m-d before either strict check runs. Verified against 12 representative inputs (ISO, non-padded, US-slash, ISO-with-time, ISO-8601-Z, human-readable, empty, garbage) — all legitimate dates now pass, invalid input still correctly rejected. 59/59 tests, phpcs/phpstan clean. Both fixes logged in BUGS.md. Live browser confirmation still outstanding (blocked); user has direct access and can confirm themselves.
- [x] 12b. Live bug (user-reported fatal error) — user hit `TypeError: Too few arguments... exactly 6 expected` on Save.php's generated Interceptor immediately after the 12a work. Root cause: the earlier 11g cleanup pass added a 6th constructor dependency (SourceRepositoryInterface) to Save.php for server-side source_code validation, but the auto-generated `generated/code/.../Save/Interceptor.php` (which mirrors constructor signatures verbatim) was never regenerated, so it still only accepted 5 args. Fix: cleared generated/code + generated/metadata and ran `bin/magento setup:di:compile` to regenerate all interceptors/factories/proxies against current constructors; confirmed the regenerated Interceptor now accepts all 6 args. cache:flush run, 59/59 tests pass. Logged in BUGS.md with an operational note that any DI constructor-signature change requires setup:di:compile before testing, distinct from the ui_component config-cache issue found earlier.
- [ ] 13. Approver
- [ ] 14. ADR
- [ ] 15. Documentation
- [ ] 16. PR

